# SCRN-BRECQ 多样本泛化评估目录

本目录用于保存 `evaluate_quantized_scrn_multi.py` 生成的多样本量化泛化评估 run。

实际 run 产物包括 `config.json`、`metrics.json`、`summary.md`、
`per_sample_metrics.jsonl` 和可选 `figures/`，默认由 `.gitignore` 忽略，不应提交。
