# Scripts

This directory will contain scripts used only for `ch4_2_exp01_w4a32_visual_recovery`.

Script requirements:

- Read experiment paths from `../experiment_info.json` when possible.
- Write candidate figures only under `../candidates/`.
- Write one `manifest_vXXX.json` for each generated candidate version.
- Never overwrite existing `vXXX` figure or manifest files.
- Record generation details in `../../../ARTIFACTS_LOG.md`.

No figure-generation script has been added yet.
